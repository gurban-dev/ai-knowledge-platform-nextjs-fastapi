[project]
name = "akp-core"
version = "0.1.0"
description = "Shared domain primitives: errors, IDs, RBAC, scopes"
requires-python = ">=3.12"
dependencies = []

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/akp_core"]
