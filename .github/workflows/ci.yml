name: CI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

concurrency:
  group: ci-${{ github.ref }}
  cancel-in-progress: true

env:
  NODE_VERSION: 24
  PYTHON_VERSION: "3.12"

jobs:
  quality-node:
    name: Node · Lint · Typecheck · Unit
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Generate Prisma client
        run: pnpm db:generate

      - name: Typecheck
        run: pnpm typecheck

      - name: Lint
        run: pnpm lint

      - name: Unit tests
        run: pnpm test

      - name: Dependency audit
        run: pnpm audit --audit-level=high || true

      - name: Build packages and apps
        run: pnpm build

  quality-python:
    name: Python · Ruff · Mypy · Pytest
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4

      - name: Setup uv
        uses: astral-sh/setup-uv@v5
        with:
          version: "0.11.33"
          enable-cache: true

      - name: Setup Python
        run: uv python install ${{ env.PYTHON_VERSION }}

      - name: Install workspace
        run: uv sync --all-packages

      - name: Ruff
        run: uv run ruff check apps packages

      - name: Mypy
        run: uv run mypy

      - name: Pytest (unit)
        env:
          NODE_ENV: test
          DATABASE_URL: postgresql://akp:akp_dev_password@localhost:5432/akp?schema=public
          REDIS_URL: redis://localhost:6379
          JWT_ACCESS_SECRET: test-access-secret-000000000000000000000
          JWT_REFRESH_SECRET: test-refresh-secret-11111111111111111111
        run: uv run pytest -q

  integration-node:
    name: Node integration (contract reference)
    runs-on: ubuntu-latest
    services:
      postgres:
        image: pgvector/pgvector:pg16
        env:
          POSTGRES_USER: akp
          POSTGRES_PASSWORD: akp_dev_password
          POSTGRES_DB: akp_test
        ports:
          - 5432:5432
        options: >-
          --health-cmd "pg_isready -U akp"
          --health-interval 5s
          --health-timeout 5s
          --health-retries 10
      redis:
        image: redis:7-alpine
        ports:
          - 6379:6379
        options: >-
          --health-cmd "redis-cli ping"
          --health-interval 5s
          --health-timeout 3s
          --health-retries 10
    env:
      NODE_ENV: test
      DATABASE_URL: postgresql://akp:akp_dev_password@localhost:5432/akp_test?schema=public
      TEST_DATABASE_URL: postgresql://akp:akp_dev_password@localhost:5432/akp_test?schema=public
      REDIS_URL: redis://localhost:6379
      JWT_ACCESS_SECRET: test-access-secret-000000000000000000000
      JWT_REFRESH_SECRET: test-refresh-secret-11111111111111111111
    steps:
      - uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4

      - name: Setup Node
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: pnpm

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Enable pgvector extension
        env:
          PGHOST: localhost
          PGPORT: 5432
          PGUSER: akp
          PGPASSWORD: akp_dev_password
          PGDATABASE: akp_test
        run: |
          psql -c "CREATE EXTENSION IF NOT EXISTS vector;"
          psql -c "CREATE EXTENSION IF NOT EXISTS pg_trgm;"

      - name: Apply migrations
        run: pnpm db:deploy

      - name: Integration tests
        run: pnpm test:integration
